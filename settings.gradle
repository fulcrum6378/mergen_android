pluginManagement { repositories { gradlePluginPortal(); google() } }
dependencyResolutionManagement { // "mavenCentral()" was removed from the both.
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories { google() }
}
rootProject.name = "Mergen"
include ':android'
